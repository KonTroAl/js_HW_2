// 2. Чему будет равен x? 

var a = 2;
var x = 1 + (a *= 2);
// x = 5, т.к. сначала выполняются операции в скобках, а дальше сложение с 1. Оператор *= умножает значение "а" на 2, что дает нам в итоге 4.